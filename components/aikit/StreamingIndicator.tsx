/**
 * StreamingIndicator — re-exported from @ainative/ai-kit (issue #6).
 *
 * Replaces the previously vendored implementation with the maintained library
 * component. Existing import paths (`@/components/aikit/StreamingIndicator`)
 * continue to work unchanged.
 */
export {
  StreamingIndicator,
  StreamingIndicator as default,
  type StreamingIndicatorProps,
} from '@ainative/ai-kit'
