/**
 * CodeBlock — re-exported from @ainative/ai-kit (issue #6).
 *
 * Replaces the previously vendored syntax-highlighting implementation with the
 * maintained library component for professional code highlighting. Existing
 * import paths (`@/components/aikit/CodeBlock`) continue to work unchanged.
 */
export {
  CodeBlock,
  CodeBlock as default,
  type CodeBlockProps,
} from '@ainative/ai-kit'
