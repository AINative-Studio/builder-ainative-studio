# Autonomous work-list — carry forward across session reset (2026-08-27 ~2am)

Standing loop: work these in priority order. For EACH: fix on a branch → gate (tsc + `npx vitest run` with dev server killed; VITEST not jest) → `pnpm build` → deploy `railway up --detach` with `&&` gating → live-verify on prod → close the issue with evidence. NO AI attribution in commits. Beware `git apply` is atomic — merge worktrees per-file. Check `docs/audits/CODEBASE_AUDIT_2026-08-27.md` before building new (wire dormant first).

## P0 — do first
1. **#350** — [CLOSED 2026-08-28, tick @6ec897a. Real fix live: c43c826 on main, prod health version=6ec897a (downstream). Verified: wantsFullDiscipline/computeAgentTurnBudget/AGENT_WALL_CLOCK_MS=240000 present + complexity threaded from chat-ws:419; agent-complexity-gating-350.test.ts 9/9 + scaffold-gate-348.test.ts 6/6 green. The 2 stray tsc errors (context-budget-logic.test.ts, ux-trace.spec.ts) pre-date c43c826, untouched by it. CODY_AGENT_PRIMARY stays UNSET.] Agent generation exhausts budget/max_tokens on a 13-min turns=1 run, ships the 263-char stub. MITIGATED (CODY_AGENT_PRIMARY unset → bedrock path primary, proven to produce real 18k-char apps). REAL FIX still needed: gate the plan/review/test/MCP prompt blocks (added this session in claude-agent.ts AGENT_SYSTEM_PROMPT + test-runner + plan-review + primitive-catalog mcpDataProvisioningBlock, ~1100 tokens) on COMPLEXITY so simple/medium apps get the lean prompt; add a ~4min wall-clock timeout to runHeadlessAgent that aborts to fallback; cap per-turn max_tokens. Only then reconsider CODY_AGENT_PRIMARY=1. Root cause is my session's build-quality features overloading the agent.
2. **#348** — [FIXED+LIVE 57164b2/cdf8c46e: scaffold-identity gate rejects the stub (422 unedited_scaffold), live-verified against aerosol chatId. CLOSED.] Unedited scaffold ('Builder Session' 263-char stub) persists as the app; gates pass valid-but-empty output. Fix: scaffold-identity gate in ready-gate (reject byte-identical/placeholder-containing App.tsx via a SCAFFOLD_FINGERPRINT), agent-produced-nothing guard (no App.tsx edit in trajectory → generation_failed), min-substance floor (<500 chars = retry), change the placeholder text to an obvious sentinel.

## Verify / cleanup
3. **aerosol** — ✅ FIXED + LIVE (2026-08-28, #352 CLOSED @de1d5b5, deploy 76d1c948). ROOT CAUSE was NOT codegen quality: the flattened-parse ready-gate parsed with plugins:['jsx'] ONLY (no typescript), so it false-rejected valid .tsx apps on TS syntax as "Truncated/unbalanced (Missing semicolon 154:30)". The preview renderer strips TS via the typescript preset + errorRecovery, so the gate was STRICTER than what the browser runs. Fix = add 'typescript' to the plugin set. Behavior-verified (health version field STALE for CLI deploys — probe behavior): identical register-app for CWak9S7vN1bqGOxoTEqH2 went 422 syntax_error → 200 ok:true; preview 62KB real app JSX, no rendered error. Aerosol now renders the real 9530-char gallery. NOTE: the earlier "codegen quality / truncation" diagnosis was WRONG — the code was valid TSX all along; the gate couldn't parse TS. Latent concern (documented in #352, not currently failing): resolveStoredApp prefers in-memory over durable — re-open scoped issue only if a real divergence surfaces. tidemark still VERIFIED GOOD.

### (superseded) prior aerosol notes
3b. **aerosol** — STILL BROKEN but gates working AS DESIGNED (2026-08-28). Isolated: generation + persistence work fine (counter build JPK6Re9T7gw9irh3Ggy6V=9.5k persisted+renders; lk3=18.8k). The regen-aerosol2.mjs script fails because its SSE socket is cut early → the generation it triggers never persists. A COMPLETE uninterrupted stream persists a real 9.5k app, but the last aerosol build hit register 422 reason=syntax_error (flattened-parse gate correctly REFUSING to point aerosol at a syntactically-broken build). So aerosol keeps generating broken apps — the gate protects users (shows honest retry, not blank/broken), and the real fix is CODEGEN QUALITY (broken/truncated multi-file output), not aerosol-specific. tidemark is FINE (verified rendering). Do NOT keep hammering aerosol regens — needs a codegen-quality pass. Use a complete-stream regen (scratchpad/aero-complete.mjs) not the socket-cut regen-aerosol2.mjs. tidemark: VERIFIED GOOD 2026-08-28 — HtjoKXT1nzxu_U-UUIy4L (21k real code) RENDERS on prod (Community Feed/Member Directory/Activity Stats, no error page, no console errors). Earlier FAIL label was stale. aerosol still points at empty UZJvTwOq6C75Xn34NIXH9 — needs a fresh regen, blocked while chat-ws returns 0 bytes (bedrock path slow/throttled; not the agent-timeout since CODY_AGENT_PRIMARY is unset). Retry regen next tick.
4. **Harness measurement study** — resume agent a7de5930dc293a9cf (was rate-limited by ANTHROPIC, transient) to finish the committee confusion matrix (#346) + staircase A/B (#345) and write docs/audits/HARNESS_MEASUREMENT_2026-08-27.md. Ensure CODY_CONTEXT_STAIRCASE_WIRED stays UNSET on prod after.

## Backlog (build when P0 clear)
4b. **#351 committee roster** — ✅ FIXED + LIVE 2026-08-28 (@9addc73, deploy c01afab3, #351 CLOSED). Roster ids qwen-2.5-coder-32b/gemini-2.5-pro were NOT in the proxy registry (400 Unknown model) → only claude reached. Fixed → ['claude-opus-4.5','qwen-coder-32b','gemini-flash'] (valid ids from GET /api/v1/models, smoke-tested 200). Behavior-verified: committee-review now succeeded:3/3, three vendors, three independent verdicts (approve/request-changes/needs-discussion). UNBLOCKS re-running the #346 measurement study (docs/audits/HARNESS_MEASUREMENT_2026-08-27.md) with a REAL 3-model committee (N≥12) — the earlier "weak predictor" verdict measured a single model and must be re-measured now.

5. **#344** — ✅ FEATURE SHIPPED + LIVE 2026-08-28 (@9a72a00, deploy 5c65bfe7). lib/growth/winback-email.ts + /api/cron/winback (Bearer $CRON_SECRET, DEFAULTS DRY-RUN — real send needs ?send=true; unsubscribe works w/o secret) + listAllApps() in app-registry. Tests 11/11. Behavior-verified: unauthed→Unauthorized, authed dry-run→{dryRun:true,candidates:0,...} (pipeline runs, sends nothing), ?unsubscribe→{ok:true}. Cody voice, single UTM CTA, 30d suppression + permanent unsubscribe via ZeroDB builder_emails, blast cap 50/run, HTML-escaped, never emails anonymous/deleted. REMAINING = OPS/FOUNDER: (1) schedule Railway cron → GET /api/cron/winback?send=true nightly w/ Bearer $CRON_SECRET; (2) set RESEND_API_KEY (+ verify sending domain); (3) first real run ?send=true&limit=1 to a test owner. Prereq (cron allowlist) was @cc88e89/438a8b4e.
6. **#347** — Trajectory fork/merge DAG. ★ ACTIVE (founder chose this 2026-08-28 — the only remaining buildable dev issue; #344 is shipped-pending-ops, #349 founder-blocked). MULTI-TICK. Plan: (1) extend TrajectoryStep/store in lib/agent/trajectory-capture.ts with parent_traj + parent_step provenance (currently a flat fire-and-forget steps[]); (2) subagent runs (subagents.ts) FORK from the parent trajectory + MERGE results back with pointers; (3) a failed validation phase re-forks from the last good step instead of dead-ending; (4) offline explain tool over cody_trajectories (LLM narrates each sub-run — closes the 'stop-reason captured but unused' gap). Start with (1)+tests (pure data model, no runtime risk), then (2), then (3), then (4). Keep each tick to one coherent slice + gate + tests. NOTE: this is fine-tuning/RLHF capture infra, mostly OFFLINE — low prod-deploy risk; deploy only slices that touch a live route.
   - ✅ SLICE 1 DONE 2026-08-28 (@7d1eabf): TrajectoryProvenance {traj_id, parent_traj, parent_step, node_role}; TrajectoryRecord extends it; storeTrajectory persists it; forkProvenance()/rootProvenance() pure+deterministic (caller supplies fork suffix); TrajectoryCapture takes optional provenance DEFAULTING to root(chatId) so all existing callers unchanged. No runtime touched (offline capture, no deploy). Tests 6/6 new + 10/10 existing trajectory tests; 0 new tsc errors; build OK.
   - ✅ SLICE 2 DONE 2026-08-28 (@bff89b8, deploy bdbdc7b8): subagents.ts captured NO trajectory at all → slice 2 = wire it. subagentForkRecord() (pure builder) + runOrchestratorAgent forks per subagent under parent traj_id: parent→[design@1,code@2,validation@3]. Fire-and-forget + try/catch (can't affect generation). Tests 6/6 new + 6/6 slice-1 + 6/6 existing; 0 new tsc errors; build OK; deployed+healthy. HONEST NOTE: prod has CAPTURE_TRAJECTORIES=true BUT USE_SUBAGENTS=false → orchestrator is NOT the live path (standard mode is), so fork-capture is tested+deployed but writes no live DAG rows until subagents mode is enabled (founder/ops toggle, not flipped). Correctness proven by unit tests, not a live row.
   - ✅ SLICE 3 DONE 2026-08-28 (@83bfba2, deploy 97b67aad): planRefork() pure decision+provenance minter (bounded, retries fork from LAST GOOD step); runOrchestratorAgent re-fork loop on validation failure — fold QA report into spec, re-run code, re-validate, bounded SUBAGENT_MAX_REFORKS=2; each re-fork captured under parent (parent_step=2, code.retryN/validation.retryN); accepted fork reward=1; extra calls only on failure. Tests 7/7 new + 18/18 prior; 0 new tsc; build OK; deployed+healthy. Dormant live (USE_SUBAGENTS=false) — tested not live-verified.
   - ✅ SLICE 4 DONE 2026-08-28 (@c5ef5c1): lib/agent/traj-explain.ts (pure DAG reconstruction, unit-tested) + scripts/traj-explain.ts (reads cody_trajectories, renders reward+stop_reason per node, optional --narrate). Legacy rows (no traj_id) → root by chat_id. SMOKE-VERIFIED LIVE: read 118 real rows, 118 trees, surfaced stop=max_tokens etc. Tests 11/11. Offline, no deploy.
   - ✅✅ #347 COMPLETE + CLOSED 2026-08-28 (all 4 slices, 30/30 tests). Dormant on live path (USE_SUBAGENTS=false) except the explain tool which works on real data today.

## ⛳ BUILDABLE BACKLOG CLEAR (2026-08-28)
All buildable dev issues in this work stream are DONE: #341/342/343 (build-quality), #345 (staircase), #346 (committee), #348 (scaffold gate), #350 (agent envelope), #351 (committee roster), #352 (aerosol gate parser), #344 (winback feature), #347 (trajectory DAG). What remains is NOT buildable-by-loop:
- **#349** — founder-blocked (Gitea epic tenancy/access decisions).
- **#344 residual** — founder/ops only (schedule Railway cron for /api/cron/winback?send=true, set RESEND_API_KEY + verify sending domain, first run ?send=true&limit=1).
- **#346 re-measurement** — ✅ DONE 2026-08-28 (@1fbc29e): real 3-vendor committee run on 3 known-PASS builds → 0/3 approved, all needs-discussion (100% false-block). VERDICT: do NOT gate with the committee; keep OFF the live gate; needs weighted/chair-arbitrated (not consensus) + calibrated prompt if ever revisited. Deterministic gates remain the shipping gate. No further measurement without those 2 changes.
- **Subagents/staircase enablement** — flipping USE_SUBAGENTS / CODY_CONTEXT_STAIRCASE_WIRED are founder/measurement calls, NOT loop actions.
→ Loop should REPORT STATUS AND STOP unless the founder points it at #346 re-measurement or unblocks #349.
7. **#349** — EPIC per-company Gitea on Railway. Needs founder decisions (org tenancy, founder git access r/w vs read-only) before implementation — do NOT start building without those answers; the epic body lists them.

## GENERATION IS FINE (false alarm corrected 2026-08-28 next tick)
Earlier "hang" was MY diagnostic error — curl -m probes were cut by their own timeout and I misread empty output as a hang. Confirmed: POST chat-ws returns http=200, first byte 0.11s, streams a real component + type:complete in ~34s (chatId JPK6Re9T7gw9irh3Ggy6V). Generation works normally. aerosol regen + measurement study are UNBLOCKED.

## Guardrails
- CODY_AGENT_PRIMARY must stay UNSET until #350's real fix lands (bedrock path is primary + reliable).
- CODY_CONTEXT_STAIRCASE_WIRED must stay UNSET (staircase inert until measured).
- If prod chat-ws returns 0 bytes: that's the 13-min agent timeout OR a transient — check Railway logs for reason=max_tokens before assuming a rate limit. It is NOT a Bedrock/AINative quota.

## #349 GITEA EPIC — ✅ ALL CHILDREN COMPLETE (2026-08-28)
Founder made all 3 decisions (org-per-workspace; LFS=existing ZeroDB/MinIO files bucket; access=read-only mirror + edits-via-Cody + optional human collaborator). All buildable children DONE:
- ✅ **GIT-0 #353 DONE + MERGED (main @4a9889a):** committee deriveVerdict → weighted/chair-arbitrated (not consensus). Tests 91/91.
- ✅ **GIT-1 #354 DONE + MERGED (main @c614fb8):** lib/git/gitea-client.ts (473 lines) + app-registry gitRepoUrl/gitRepoId/gitOrg fields. Tests 16/16.
- ✅ **GIT-2 #355 DONE + MERGED (main @7971307):** lib/git/company-repo.ts (314 lines) — provisionCompanyRepo, commitRegeneration, grantHumanWrite + pure helpers. Tests 27/27.
- ✅ **GIT-3 #356 DONE + MERGED (main @91b22b8):** lib/git/task-git-sync.ts (342 lines) — task→branch/commit/PR flow. Tests 35/35.
- ✅ **GIT-5 #357 DONE + MERGED (main @1a1ab16):** coding-standards.ts enforcement checks (checkCommitMessage, checkTestCoverage, checkTddCompliance, etc). Tests 65/65.

**TOTAL: 143 new tests across the Gitea epic.**

REMAINING (NOT BUILDABLE — founder/ops):
- Live Gitea provisioning: `railway up` a Gitea service + Postgres → set GITEA_BASE_URL + GITEA_ADMIN_TOKEN
- Scope #4 (committee-gated PRs): wire committee verdict to PR merge approval (needs live Gitea)
- Builder CI: add check-standards to GitHub Actions required status
